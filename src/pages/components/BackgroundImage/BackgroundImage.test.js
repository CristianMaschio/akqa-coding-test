import React from "react";
import { shallow } from "enzyme";
import BackgroundImage from ".";

describe("Test component: <BackgroundImage />", () => {
  it("should BackgroundImage render!", () => {
    shallow(<BackgroundImage />);
  });
});
